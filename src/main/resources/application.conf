akka {
  loglevel = DEBUG
  log-dead-letter = false
  log-dead-letters-during-shutdown = false
  actor {
    debug {
      # enable function of LoggingReceive, which is to log any received message at
      # DEBUG level
      receive = on
    }
  }
}
